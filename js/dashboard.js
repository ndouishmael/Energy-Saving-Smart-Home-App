// JavaScript code for user dashboard functionality
// You can add any desired functionality here

// Example: Display a welcome message to the user
var user = {
  name: "John Doe",
  email: "john@example.com",
  role: "User"
};

var welcomeMessage = "Welcome, " + user.name + "! You are logged in as a " + user.role;
console.log(welcomeMessage);
